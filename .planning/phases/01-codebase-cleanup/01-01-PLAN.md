---
phase: 01-codebase-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/app/layout.tsx
  - src/components/GradientBackground.tsx
  - src/components/InfoItem.tsx
  - src/components/sections/IntroduceSection.tsx
  - src/components/sections/ProjectSection.tsx
  - src/components/BaseVideo.tsx
  - src/components/BaseImage.tsx
  - next.config.ts
  - .env.local
  - .env.production
autonomous: true
requirements:
  - FNDN-01
  - FNDN-02
  - UX-05

must_haves:
  truths:
    - "All components using React hooks, framer-motion, event handlers, or browser APIs have 'use client' directives"
    - "page.tsx is a server component (no 'use client' directive) and composes client section components"
    - "No references to NEXT_PUBLIC_BASE_PATH remain in any source file or env file"
    - "No references to quanmofii.github.io remain -- all URLs point to rayquasar18.github.io"
    - "CV download button path /HaMinhQuan_CV.pdf is not broken by basePath removal"
    - "next.config.ts uses proper TypeScript ESM syntax with no basePath or assetPrefix"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Server component that composes client sections"
      contains: "export default function Home"
    - path: "src/components/GradientBackground.tsx"
      provides: "Mouse-following gradient (client component)"
      contains: "use client"
    - path: "src/components/InfoItem.tsx"
      provides: "Hover-reveal info item (client component)"
      contains: "use client"
    - path: "src/components/sections/IntroduceSection.tsx"
      provides: "Introduce section (client component)"
      contains: "use client"
    - path: "src/components/sections/ProjectSection.tsx"
      provides: "Projects section (client component)"
      contains: "use client"
    - path: "next.config.ts"
      provides: "Simplified Next.js config for root domain static export"
      contains: "export default"
    - path: "src/components/BaseVideo.tsx"
      provides: "Video component using direct src path"
    - path: "src/components/BaseImage.tsx"
      provides: "Image component using direct src path"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/sections/*.tsx"
      via: "import and render"
      pattern: "import.*from.*sections/"
    - from: "src/components/BaseVideo.tsx"
      to: "public/hero/video1.webm"
      via: "direct path (no basePath)"
      pattern: "src={src}"
    - from: "next.config.ts"
      to: "static export"
      via: "output: export"
      pattern: "output.*export"
---

<objective>
Restore all source files from git, fix missing "use client" directives on 4 components, convert page.tsx to server component, and remove all basePath/URL legacy configuration.

Purpose: Eliminate hydration errors from missing client directives and fix the broken asset/URL configuration so the site builds and deploys correctly to rayquasar18.github.io as a root domain.
Output: All source files restored, client/server boundaries correct, next.config.ts simplified, all URLs updated.
</objective>

<execution_context>
@/Users/mqcbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mqcbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-codebase-cleanup/01-CONTEXT.md
@.planning/phases/01-codebase-cleanup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore source files from git and install dependencies</name>
  <files>
    src/app/page.tsx, src/app/layout.tsx, src/app/globals.css, src/app/_config.yml,
    src/app/fonts/MarlinGeoSQ-Medium.woff, src/app/fonts/MarlinGeoSQ-Regular.woff,
    src/app/fonts/Saprona-Light.woff, src/app/fonts/Saprona-Medium.woff, src/app/fonts/Saprona-Regular.woff,
    src/components/AnimatedDiv.tsx, src/components/AnimatedText.tsx,
    src/components/BaseImage.tsx, src/components/BaseVideo.tsx,
    src/components/ButtonDownloadCV.tsx, src/components/ButtonRedirect.tsx,
    src/components/Footer.tsx, src/components/GradientBackground.tsx,
    src/components/Header.tsx, src/components/InfoItem.tsx, src/components/LoadingScreen.tsx,
    src/components/sections/AboutSection.tsx, src/components/sections/HeroSection.tsx,
    src/components/sections/IntroduceSection.tsx, src/components/sections/ProjectSection.tsx,
    .env.local, .env.production, eslint.config.mjs, package.json, package-lock.json,
    next.config.ts, public/*, README.md
  </files>
  <action>
    All source files are deleted from the working tree but exist in git history. Restore them:

    1. Run `git checkout HEAD -- .` to restore ALL tracked files from the latest commit.
    2. Run `npm install` to restore node_modules from the package-lock.json.
    3. Verify the restore: run `ls src/app/page.tsx src/components/LoadingScreen.tsx next.config.ts` to confirm files exist.
    4. Run `npm run build` to confirm the project builds in its current (buggy) state -- this is the baseline. If it fails, that is expected and acceptable (it confirms the bugs we are fixing).

    Do NOT modify any files in this task -- just restore and install.
  </action>
  <verify>
    <automated>ls src/app/page.tsx src/components/LoadingScreen.tsx next.config.ts src/components/GradientBackground.tsx && echo "Files restored"</automated>
  </verify>
  <done>All source files are back on disk. node_modules installed. Ready for modifications.</done>
</task>

<task type="auto">
  <name>Task 2: Fix "use client" directives and convert page.tsx to server component</name>
  <files>
    src/app/page.tsx,
    src/components/GradientBackground.tsx,
    src/components/InfoItem.tsx,
    src/components/sections/IntroduceSection.tsx,
    src/components/sections/ProjectSection.tsx
  </files>
  <action>
    Fix the client/server component boundary per user decision for SEO and loading performance:

    **Add `"use client";` as the FIRST line to these 4 files (they use hooks/framer-motion but are missing the directive):**

    1. `src/components/GradientBackground.tsx` -- uses useEffect, useRef, useMotionValue, useSpring, useTransform from framer-motion
    2. `src/components/InfoItem.tsx` -- uses useState, motion from framer-motion
    3. `src/components/sections/IntroduceSection.tsx` -- uses useRef, useInView, motion from framer-motion
    4. `src/components/sections/ProjectSection.tsx` -- uses useRef (and imports client components GradientBackground, AnimatedDiv, BaseImage)

    **Remove `"use client";` from `src/app/page.tsx`:**

    page.tsx currently has `"use client"` at the top but it does NOT use any hooks, event handlers, or browser APIs. It only imports and renders child components. Remove the `"use client"` directive so it becomes a server component. Each child section component already has (or will now have) its own `"use client"` directive.

    The result: page.tsx is a server component (better for SEO, smaller client bundle). All 4 components that genuinely need client features have their directives.

    **Verification approach:** After making changes, check that NO component using React hooks or framer-motion is missing `"use client"`. Grep for `import.*from "react"` and `from "framer-motion"` patterns across all component files, and verify each matching file has `"use client"`.
  </action>
  <verify>
    <automated>grep -rL '"use client"' src/components/GradientBackground.tsx src/components/InfoItem.tsx src/components/sections/IntroduceSection.tsx src/components/sections/ProjectSection.tsx | wc -l | tr -d ' ' | grep -q '^0$' && ! grep -q '"use client"' src/app/page.tsx && echo "PASS: All directives correct" || echo "FAIL"</automated>
  </verify>
  <done>GradientBackground, InfoItem, IntroduceSection, and ProjectSection all have "use client". page.tsx is a server component (no "use client" directive).</done>
</task>

<task type="auto">
  <name>Task 3: Remove basePath/URL legacy and update all domain references</name>
  <files>
    next.config.ts,
    .env.local,
    .env.production,
    src/components/BaseVideo.tsx,
    src/components/BaseImage.tsx,
    src/app/layout.tsx
  </files>
  <action>
    Per user decision: deploy to root domain rayquasar18.github.io -- no basePath needed.

    **1. Rewrite `next.config.ts` to proper TypeScript ESM (no basePath/assetPrefix):**
    ```typescript
    import type { NextConfig } from "next";

    const nextConfig: NextConfig = {
      reactStrictMode: true,
      images: {
        unoptimized: true,
      },
      trailingSlash: true,
      output: "export",
    };

    export default nextConfig;
    ```
    Remove the `isProd` conditional, `prefix` variable, `basePath`, and `assetPrefix` properties entirely. Convert from `module.exports` (CommonJS) to `export default` (ESM) since the file extension is `.ts`.

    **2. Remove NEXT_PUBLIC_BASE_PATH from both env files:**
    - `.env.local`: delete the `NEXT_PUBLIC_BASE_PATH=` line (or delete the file if that is the only content)
    - `.env.production`: delete the `NEXT_PUBLIC_BASE_PATH=` line (or delete the file if that is the only content)

    **3. Simplify `src/components/BaseVideo.tsx` -- remove basePath logic:**
    Remove the two lines: `const basePath = process.env.NEXT_PUBLIC_BASE_PATH || ''` and `const fullSrc = \`\${basePath}\${src}\``. Use `src` directly in the video element's `src` prop.

    **4. Simplify `src/components/BaseImage.tsx` -- remove basePath logic:**
    Same pattern: remove `basePath` and `fullSrc` variables. Use `src` directly in the Image component's `src` prop.

    **5. Update all `quanmofii.github.io` references in `src/app/layout.tsx`:**
    Replace every occurrence of `quanmofii.github.io` with `rayquasar18.github.io` (case-insensitive search -- also check for `QuanMofii.github.io` in the JSON-LD block). The affected locations are:
    - `metadataBase: new URL("https://quanmofii.github.io/")`
    - `authors[0].url`
    - `openGraph.url`
    - `openGraph.images[0].url`
    - `twitter.images[0]`
    - JSON-LD `url`, `image`, `sameAs` array entries

    For the `sameAs` array GitHub URL, also update from `https://github.com/QuanMofii` to `https://github.com/rayquasar18` (matching the new GitHub username). Leave LinkedIn and Twitter URLs unchanged unless they also use the old username.

    **6. Final grep to confirm no stale references remain:**
    Run `grep -ri "quanmofii" src/ next.config.ts .env*` and `grep -ri "BASE_PATH" src/ next.config.ts .env*` -- both should return empty.
  </action>
  <verify>
    <automated>grep -ri "quanmofii" src/ next.config.ts 2>/dev/null | wc -l | tr -d ' ' | grep -q '^0$' && grep -ri "BASE_PATH" src/ next.config.ts .env.local .env.production 2>/dev/null | wc -l | tr -d ' ' | grep -q '^0$' && grep -q "export default" next.config.ts && echo "PASS: All legacy removed" || echo "FAIL"</automated>
  </verify>
  <done>next.config.ts is clean ESM with no basePath/assetPrefix. BaseVideo and BaseImage use direct paths. All URLs point to rayquasar18.github.io. No NEXT_PUBLIC_BASE_PATH references remain anywhere. CV download path /HaMinhQuan_CV.pdf is unaffected (ButtonDownloadCV already hardcodes it correctly).</done>
</task>

</tasks>

<verification>
Run the full build to confirm all fixes work together:

```bash
npx next build
```

This static export build will catch:
- Missing "use client" directives (server component using hooks = build error)
- Broken imports after page.tsx becomes server component
- Invalid next.config.ts syntax

Additionally:
```bash
grep -ri "quanmofii" src/ next.config.ts .env* 2>/dev/null
grep -ri "BASE_PATH" src/ next.config.ts .env* 2>/dev/null
```
Both should return nothing.
</verification>

<success_criteria>
1. `npx next build` completes without errors
2. No "quanmofii" references in src/ or config files
3. No "BASE_PATH" references in src/ or config/env files
4. page.tsx has no "use client" directive
5. GradientBackground, InfoItem, IntroduceSection, ProjectSection all have "use client"
6. next.config.ts uses `export default` (ESM) with no basePath/assetPrefix
</success_criteria>

<output>
After completion, create `.planning/phases/01-codebase-cleanup/01-01-SUMMARY.md`
</output>
